# GraphQL server URL — change to your deployed URL when you go live
VITE_GRAPHQL_URL=http://localhost:4000/graphql

# Anthropic API key — used only by the Teacher Help Widget (Mr. van der Berg)
# Get yours at https://console.anthropic.com/
# Never commit the real key — add it to .env.local (which is gitignored)
VITE_ANTHROPIC_API_KEY=
